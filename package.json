{
  "name": "chatserver",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "npx prisma validate && npx prisma generate && nodemon server.js",
    "build": "npx prisma validate && npx prisma generate && nodemon server.js "
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.16.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "prisma": "^5.16.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
